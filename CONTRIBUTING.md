# Contributing

1. Create a branch with the `codex/` prefix.
2. Keep changes scoped and include tests.
3. Append task/test events to `ops/master-log.ndjson`.
4. Ensure CI passes: typecheck, test, build, openapi-drift.
